<?xml version="1.0"?>

<!DOCTYPE todo SYSTEM "./dtd/todo-v10.dtd">

<todo title="JAMES Things To Do">
 <devs>
    <person name="Federico Barbieri" email="scoobie@systemy.it" id="FB"/>
    <person name="Serge Knystautas" email="sergek@lokitech.com" id="SK"/>
    <person name="Charles Benett" email="charles@benett1.demon.co.uk" id="CB"/>
 </devs>

<!-- Actions are grouped by priority
     Each action has:
         a context, currently code or docs
         a description
        (optionally) an estimate of amount of work (small, some, lots)
-->

  <actions priority="high">
    <action context="docs">
      Port project to Jakarta
    </action>
    <action context="docs">
      Write all documentation.
    </action>
    <action context="code">
      Allow RemoteManager to change passwords of users (change attribute)
      <suggester>sanjeev ankem sanjeev_a@rediffmail.com</suggester>
    </action>
    <action context="code">
      Let RemoteDelivery send all messages to a single remote server (like a gateway)
    </action>
    <action context="code">
      Improve exception management in RemoteDelivery mailet.
    </action>
    <action context="code">
      Add support for regex inside most matchers and thereby consolidate a few of them.
    </action>
  </actions>

  <actions priority="medium">
    <action context="code">
      Make James stoppable (elegantly)
    </action>
    <action context="code">
      Get IMAP server to alpha standard, ie basic interoperation with e-mail clients.
      <work>lots</work>
    </action>
    <action context="code">
      Add #news namespace to IMAP system
      <work>some</work>
    </action>
    <action context="code">
      Revisit UserRepository - after doing IMAP List & Subscribe
      <work>some</work>
    </action>
    <action context="code">
      Make James reconfigurable/contextualizable/composable.
      (What does this mean?? New Avalon thing? - SK)
    </action>
    <action context="code">
      Reinject error/spam mails incorrecty handled.
    </action>
    <action context="code">
      Add needed functions to RemoteManager.
      Including Stop and ReConfigure.
      (What else? - CB)
      Reinject mail.
      Store RemoteManger password securely.
    </action>
    <action context="code">
      Add support for better mailet router/processing (maybe like RequestDispatcher) - Use Stage/Pipline pattern
      <work>some</work>
    </action>
    <action context="code">
      Add support for deployable message processing apps using Camelot pattern
      <work>lots</work>
    </action>
   </actions>

  <actions priority="low">
    <action context="code">
      Write an NNTP server
      <work>lots</work>
    </action>
    <action context="code">
      Add support for DRAC login/relay allowing
     (Status?? - CB)
     (Nothing started - SK)
    </action>
    <action context="code">
      Review threadpooling - is it dynamic?
    </action>
  </actions>


</todo>