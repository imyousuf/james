<?xml version="1.0"?>
<!--
                                README!

    All blocks have full defaults so nothing is needed to make them run.

-->
<avalon-demo>

    <!-- The Storage block -->
    <block class="org.apache.avalon.demos.SimpleServer" 
           name="demoblock" >

        <logger name="Demo" destinationURL="../logs/demo.log" priority="DEBUG"/>

	<!-- The names here must match the names specified for a Block in some conf.xml file.
	    The role should be the interface mentioned in the Demo.xinfo  -->

        <provide name="sockets" role="org.apache.avalon.services.SocketServer"/>
        <provide name="objectstorage" role="org.apache.avalon.services.Store"/>

	<!-- Note trailing file searator in destinationURL -->
        <configuration>
	    <repository destinationURL="file://../var/demo/"
                        type="OBJECT"
                        model="SYNCHRONOUS">
            </repository>
	    <port>4556</port>
        </configuration>

    </block>


</avalon-demo>




